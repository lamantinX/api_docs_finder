# API Documentation Finder

Автоматический поиск документации для API методов с использованием прямой проверки OpenAPI и множественных поисковых запросов через SerpAPI.

## Описание

API Documentation Finder - это асинхронное Python-приложение, которое обрабатывает списки API методов из CSV или JSON файлов и автоматически находит соответствующую документацию. Агент использует многоуровневую стратегию поиска:

1. **Прямая проверка OpenAPI** - проверка стандартных путей OpenAPI/Swagger документации (`/openapi.json`, `/openapi.yaml`, `/swagger.json`, `/swagger.yaml`, `/api-docs`, `/redoc`)
2. **Множественный поиск через SerpAPI** - если OpenAPI не найден, выполняется 4 разных поисковых запроса:
   - Google Lite: "{name} {method} api documentation link"
   - Google Lite: "{name} {method_link} api documentation"
   - Google AI: "{name} {method} api documentation link"
   - Google AI: "{name} {method_link} api documentation"

## Возможности

- ⚡ Асинхронная обработка множества API методов
- 🔍 Приоритет OpenAPI/Swagger документации
- 🌐 4 разные поисковые стратегии через SerpAPI
- 📊 Прогресс-бар в реальном времени
- 🛡️ Отказоустойчивость - ошибки отдельных методов не останавливают процесс
- 📁 Поддержка CSV и JSON входных форматов
- 💾 Сохранение результатов в CSV и JSON

## Установка

### Требования

- Python 3.7 или выше
- pip (менеджер пакетов Python)

### Шаги установки

1. Клонируйте репозиторий или скопируйте файлы проекта:
```bash
git clone <repository-url>
cd api-doc-finder
```

2. (Рекомендуется) Создайте виртуальное окружение:
```bash
python -m venv venv

# Windows
venv\Scripts\activate

# Linux/Mac
source venv/bin/activate
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

Зависимости включают:
- `aiohttp>=3.9.0` - асинхронный HTTP клиент
- `tqdm>=4.66.0` - прогресс-бар

**Примечание:** SerpAPI ключ уже встроен в приложение, дополнительная настройка не требуется.

## Использование

### Базовое использование

```bash
python main.py --input input.csv
```

Это запустит агент с входным файлом `input.csv` и создаст выходные файлы `results.csv` и `results.json` в текущей директории.

### Параметры командной строки

| Параметр | Обязательный | По умолчанию | Описание |
|----------|--------------|--------------|----------|
| `--input` | Да | - | Путь к входному CSV или JSON файлу с API методами |
| `--output` | Нет | `results` | Базовое имя для выходных файлов (без расширения) |
| `--help` | Нет | - | Показать справку по использованию |

### Примеры использования

**Базовый пример с CSV:**
```bash
python main.py --input input.csv
```
Создаст: `results.csv` и `results.json`

**С JSON входом:**
```bash
python main.py --input input.json
```
Создаст: `results.csv` и `results.json`

**С пользовательским именем выходных файлов:**
```bash
python main.py --input input.csv --output my_results
```
Создаст: `my_results.csv` и `my_results.json`

**С указанием пути к директории:**
```bash
python main.py --input data/api_methods.csv --output data/results
```
Создаст: `data/results.csv` и `data/results.json`

**Получить справку:**
```bash
python main.py --help
```

## Формат входного файла

Агент поддерживает два формата входных файлов: CSV и JSON. Формат определяется автоматически по расширению файла.

### CSV формат (`input.csv`)

CSV файл должен содержать заголовок с тремя обязательными колонками:

| Колонка | Описание | Пример |
|---------|----------|--------|
| `name` | Название API сервиса | `HubSpot`, `Slack`, `GitHub` |
| `method` | Название метода API | `Create contact`, `Send message` |
| `method_link` | URL метода API | `https://api.hubapi.com/crm/v3/objects/contacts` |

**Пример файла:**
```csv
name,method,method_link
HubSpot,Create new contact,https://api.hubapi.com/crm/v3/objects/contacts
HubSpot,Find contact,https://api.hubapi.com/crm/v3/objects/contacts/search
Slack,Send a message to a public channel,https://slack.com/api/chat.postMessage
Slack,Find a user by Email,https://slack.com/api/users.lookupByEmail
GitHub,List users,https://api.github.com/users
```

**Важно:**
- Первая строка должна содержать заголовки: `name,method,method_link`
- Все три поля обязательны для каждой записи
- Записи с пустыми полями будут пропущены с предупреждением
- Кодировка файла: UTF-8

### JSON формат (`input.json`)

JSON файл должен содержать массив объектов с тремя обязательными полями:

| Поле | Тип | Описание | Пример |
|------|-----|----------|--------|
| `name` | string | Название API сервиса | `"HubSpot"` |
| `method` | string | Название метода API | `"Create contact"` |
| `method_link` | string | URL метода API | `"https://api.hubapi.com/..."` |

**Пример файла:**
```json
[
  {
    "name": "HubSpot",
    "method": "Create new contact",
    "method_link": "https://api.hubapi.com/crm/v3/objects/contacts"
  },
  {
    "name": "HubSpot",
    "method": "Find contact",
    "method_link": "https://api.hubapi.com/crm/v3/objects/contacts/search"
  },
  {
    "name": "Slack",
    "method": "Send a message to a public channel",
    "method_link": "https://slack.com/api/chat.postMessage"
  },
  {
    "name": "Slack",
    "method": "Find a user by Email",
    "method_link": "https://slack.com/api/users.lookupByEmail"
  },
  {
    "name": "GitHub",
    "method": "List users",
    "method_link": "https://api.github.com/users"
  }
]
```

**Важно:**
- Файл должен содержать массив (начинается с `[` и заканчивается `]`)
- Каждый объект должен иметь все три поля: `name`, `method`, `method_link`
- Объекты с отсутствующими полями будут пропущены с предупреждением
- Кодировка файла: UTF-8

## Формат выходных файлов

Агент создает два выходных файла с одинаковыми данными в разных форматах: CSV и JSON.

### Структура выходных данных

| Поле | Описание | Когда заполняется |
|------|----------|-------------------|
| `name` | Название API сервиса (из входного файла) | Всегда |
| `method` | Название метода API (из входного файла) | Всегда |
| `method_link` | URL метода API (из входного файла) | Всегда |
| `openapi_link` | Найденная OpenAPI/Swagger документация | Если найдена через прямую проверку |
| `search_method_name` | Результат Google Lite поиска по имени метода | Если OpenAPI не найден |
| `search_method_link` | Результат Google Lite поиска по ссылке метода | Если OpenAPI не найден |
| `ai_method_name` | Результат Google AI поиска по имени метода | Если OpenAPI не найден |
| `ai_method_link` | Результат Google AI поиска по ссылке метода | Если OpenAPI не найден |

### CSV формат (`results.csv`)

**Пример файла:**
```csv
name,method,method_link,openapi_link,search_method_name,search_method_link,ai_method_name,ai_method_link
HubSpot,Create contact,https://api.hubapi.com/crm/v3/objects/contacts,https://api.hubapi.com/openapi.json,,,,
Slack,Send message,https://slack.com/api/chat.postMessage,,https://api.slack.com/docs,https://api.slack.com/methods/chat.postMessage,https://api.slack.com/docs,https://api.slack.com/methods/chat.postMessage
GitHub,List users,https://api.github.com/users,,error,https://docs.github.com/rest/users,https://docs.github.com/rest,error
```

**Примечания:**
- Пустые поля означают, что данные не были получены (например, OpenAPI найден, поиск не выполнялся)
- Значение `error` означает, что при получении данных произошла ошибка (таймаут, сетевая ошибка, нет результатов)

### JSON формат (`results.json`)

**Пример файла:**
```json
[
  {
    "name": "HubSpot",
    "method": "Create contact",
    "method_link": "https://api.hubapi.com/crm/v3/objects/contacts",
    "openapi_link": "https://api.hubapi.com/openapi.json",
    "search_method_name": "",
    "search_method_link": "",
    "ai_method_name": "",
    "ai_method_link": ""
  },
  {
    "name": "Slack",
    "method": "Send message",
    "method_link": "https://slack.com/api/chat.postMessage",
    "openapi_link": "",
    "search_method_name": "https://api.slack.com/docs",
    "search_method_link": "https://api.slack.com/methods/chat.postMessage",
    "ai_method_name": "https://api.slack.com/docs",
    "ai_method_link": "https://api.slack.com/methods/chat.postMessage"
  },
  {
    "name": "GitHub",
    "method": "List users",
    "method_link": "https://api.github.com/users",
    "openapi_link": "",
    "search_method_name": "error",
    "search_method_link": "https://docs.github.com/rest/users",
    "ai_method_name": "https://docs.github.com/rest",
    "ai_method_link": "error"
  }
]
```

**Примечания:**
- Пустые строки (`""`) означают, что данные не были получены
- Значение `"error"` означает, что при получении данных произошла ошибка

## Производительность

### Характеристики

- **Параллельная обработка:** До 20 одновременных HTTP запросов
- **Скорость:** ~10-15 секунд для 100 API методов (зависит от сети)
- **Оптимизация трафика:** HEAD запросы перед GET для проверки OpenAPI
- **Экономия на SerpAPI:** Приоритет OpenAPI минимизирует использование платного API

### Примерное время обработки

| Количество методов | Время (при 50% OpenAPI найдено) | SerpAPI запросов |
|-------------------|----------------------------------|------------------|
| 10 методов | ~2-3 секунды | 20 |
| 50 методов | ~5-7 секунд | 100 |
| 100 методов | ~10-15 секунд | 200 |
| 500 методов | ~50-75 секунд | 1000 |
| 1000 методов | ~100-150 секунд | 2000 |

**Примечание:** Время зависит от:
- Скорости интернет-соединения
- Времени ответа API серверов
- Процента найденных OpenAPI документаций
- Нагрузки на SerpAPI

### Оптимизация затрат на SerpAPI

SerpAPI - платный сервис. Агент оптимизирован для минимизации затрат:

1. **Приоритет OpenAPI:** Бесплатная прямая проверка выполняется первой
2. **Пропуск поиска:** Если OpenAPI найден, 4 платных запроса не выполняются
3. **Эффективность:** При 50% успешности OpenAPI экономия составляет 50% запросов

**Пример расчета затрат:**
```
Входные данные: 1000 API методов
OpenAPI найдено: 400 методов (40%)
OpenAPI не найдено: 600 методов (60%)

SerpAPI запросов: 600 × 4 = 2400 запросов
Без оптимизации было бы: 1000 × 4 = 4000 запросов
Экономия: 1600 запросов (40%)
```

### Ограничения

- **Максимум 20 одновременных соединений:** Предотвращает перегрузку сети и блокировку по IP
- **Таймаут 10 секунд:** Медленные серверы будут помечены как "error"
- **Лимиты SerpAPI:** Зависят от вашего тарифного плана SerpAPI

## Устранение неполадок

### Проблемы с входным файлом

#### Ошибка: "Input file not found"

**Причина:** Файл не существует по указанному пути

**Решение:**
```bash
# Проверьте, что файл существует
dir input.csv

# Или используйте абсолютный путь
python main.py --input C:\Users\YourName\Documents\input.csv
```

#### Ошибка: "Unsupported file format"

**Причина:** Файл имеет неподдерживаемое расширение

**Решение:** Используйте только `.csv` или `.json` файлы
```bash
# Правильно
python main.py --input input.csv
python main.py --input input.json

# Неправильно
python main.py --input input.txt
python main.py --input input.xlsx
```

#### Ошибка: "CSV file missing required columns"

**Причина:** CSV файл не содержит обязательные колонки

**Решение:** Убедитесь, что первая строка CSV содержит заголовки:
```csv
name,method,method_link
YourAPI,YourMethod,https://api.example.com/method
```

#### Предупреждение: "Skipping row X - missing required fields"

**Причина:** Строка содержит пустые обязательные поля

**Решение:** Проверьте, что все три поля заполнены:
```csv
# Неправильно (пустой method_link)
Slack,Send message,

# Правильно
Slack,Send message,https://slack.com/api/chat.postMessage
```

### Проблемы с выполнением

#### Ошибка: "ModuleNotFoundError: No module named 'aiohttp'"

**Причина:** Зависимости не установлены

**Решение:**
```bash
pip install -r requirements.txt
```

#### Программа зависает или работает очень медленно

**Причина:** Медленное сетевое соединение или большое количество методов

**Решение:**
- Проверьте подключение к интернету
- Разбейте большой входной файл на несколько меньших
- Дождитесь завершения (прогресс-бар показывает текущее состояние)

### Проблемы с результатами

#### В результатах появляется "error" в полях

**Возможные причины и решения:**

1. **Сетевые ошибки:**
   - Проверьте подключение к интернету
   - Проверьте, что `method_link` URL доступен:
     ```bash
     curl -I https://api.example.com/method
     ```

2. **Таймаут (10 секунд):**
   - Сервер API слишком медленно отвечает
   - Это нормально для некоторых API, агент продолжит работу

3. **SerpAPI недоступен:**
   - Проверьте статус SerpAPI: https://serpapi.com/status
   - Проверьте лимиты API ключа
   - Подождите и попробуйте снова

4. **Нет результатов поиска:**
   - API может быть малоизвестным
   - Документация может отсутствовать в публичном доступе
   - Попробуйте найти документацию вручную

#### Все поля поиска пустые, но openapi_link тоже пустой

**Причина:** Произошла критическая ошибка при обработке метода

**Решение:**
- Проверьте логи в консоли (сообщения с ❌)
- Проверьте корректность `method_link` URL
- Попробуйте запустить агент снова для этого метода

#### OpenAPI документация найдена, но она неправильная

**Причина:** Сервер вернул статус 200 для стандартного пути, но это не OpenAPI документация

**Решение:**
- Это редкий случай, но возможен
- Используйте результаты поиска из полей `search_*` и `ai_*`
- Проверьте найденный `openapi_link` вручную

### Проблемы с выходными файлами

#### Ошибка: "Permission denied" при записи результатов

**Причина:** Нет прав на запись в директорию

**Решение:**
```bash
# Укажите другую директорию для вывода
python main.py --input input.csv --output C:\Users\YourName\Documents\results

# Или запустите с правами администратора (Windows)
# Правый клик на cmd → "Запуск от имени администратора"
```

#### Выходные файлы не создаются

**Причина:** Ошибка в пути или имени файла

**Решение:**
- Убедитесь, что директория существует
- Не используйте специальные символы в имени файла
- Проверьте логи в консоли на наличие ошибок

### Получение помощи

Если проблема не решена:

1. Проверьте логи в консоли (сообщения с ❌)
2. Запустите с минимальным входным файлом (1-2 метода) для тестирования
3. Проверьте версии зависимостей:
   ```bash
   pip list | findstr aiohttp
   pip list | findstr tqdm
   ```
4. Убедитесь, что используете Python 3.7+:
   ```bash
   python --version
   ```

## Структура проекта

```
api-doc-finder/
├── main.py                 # Точка входа и оркестрация
├── cli.py                  # Парсинг аргументов командной строки
├── loader.py               # Загрузка CSV/JSON входных файлов
├── http_client.py          # Обертка асинхронного HTTP клиента
├── openapi_search.py       # Прямая проверка OpenAPI документации
├── serp_search.py          # Множественный поиск через SerpAPI
├── processor.py            # Пайплайн обработки API методов
├── logger.py               # Логирование прогресса
├── output.py               # Сохранение результатов в CSV/JSON
├── requirements.txt        # Зависимости Python
└── README.md              # Документация
```

## Как это работает

### Стратегия поиска документации

Агент использует двухуровневую стратегию поиска с приоритетом OpenAPI:

#### 1. Приоритет: Прямая проверка OpenAPI/Swagger

Для каждого API метода агент сначала пытается найти OpenAPI/Swagger документацию напрямую:

1. Извлекает базовый домен из `method_link` (например, `https://api.slack.com` из `https://api.slack.com/api/chat.postMessage`)
2. Проверяет стандартные пути документации в следующем порядке:
   - `/openapi.json`
   - `/openapi.yaml`
   - `/swagger.json`
   - `/swagger.yaml`
   - `/api-docs`
   - `/redoc`
3. Для каждого пути выполняет HTTP HEAD запрос (быстрая проверка)
4. Если HEAD возвращает статус 200, документация найдена
5. Если найдено, заполняется поле `openapi_link` и поиск через SerpAPI **пропускается**

**Преимущества:**
- Быстрая проверка (HEAD запросы)
- Не использует платный SerpAPI
- Высокая точность для API с стандартной документацией

#### 2. Fallback: Множественный поиск через SerpAPI

Если OpenAPI документация не найдена, агент выполняет 4 разных поисковых запроса через SerpAPI:

| Запрос | Движок | Формат запроса | Результат в поле |
|--------|--------|----------------|------------------|
| 1 | Google Lite | `"{name} {method} api documentation link"` | `search_method_name` |
| 2 | Google Lite | `"{name} {method_link} api documentation"` | `search_method_link` |
| 3 | Google AI | `"{name} {method} api documentation link"` | `ai_method_name` |
| 4 | Google AI | `"{name} {method_link} api documentation"` | `ai_method_link` |

**Пример запросов для Slack:**
```
1. "Slack Send message api documentation link" (Google Lite)
2. "Slack https://slack.com/api/chat.postMessage api documentation" (Google Lite)
3. "Slack Send message api documentation link" (Google AI)
4. "Slack https://slack.com/api/chat.postMessage api documentation" (Google AI)
```

**Преимущества:**
- Множественные стратегии увеличивают шансы найти документацию
- Google Lite - быстрый базовый поиск
- Google AI - более точный AI-powered поиск
- Разные формулировки запросов покрывают больше случаев

#### 3. Асинхронная обработка

- Все API методы обрабатываются параллельно (до 20 одновременных запросов)
- Ошибки отдельных методов не останавливают общий процесс
- Прогресс отображается в реальном времени

### Пример работы

```
Входной файл: 100 API методов

┌─────────────────────────────────────┐
│ Загрузка input.csv                  │
└─────────────────────────────────────┘
              ↓
┌─────────────────────────────────────┐
│ Параллельная обработка (20 потоков)│
└─────────────────────────────────────┘
              ↓
    ┌─────────────────┐
    │ Метод 1         │
    │ ↓               │
    │ OpenAPI? ✓      │ → openapi_link заполнен
    │ (найдено)       │   поиск пропущен
    └─────────────────┘
    
    ┌─────────────────┐
    │ Метод 2         │
    │ ↓               │
    │ OpenAPI? ✗      │ → 4 SerpAPI запроса
    │ (не найдено)    │   все поля поиска заполнены
    └─────────────────┘
              ↓
┌─────────────────────────────────────┐
│ Сохранение results.csv + results.json│
└─────────────────────────────────────┘

Результат:
- 40 методов: OpenAPI найден (0 SerpAPI запросов)
- 60 методов: OpenAPI не найден (240 SerpAPI запросов)
- Всего SerpAPI запросов: 240
```

## Лицензия

MIT
